import type { NavbarItem } from "~/global/types/Navbar/NavbarItem";

export const navbarMockData = (): NavbarItem[] => {
    return [
        {
            title: 'Works',
        },
        {
            title: 'About',
        },
    ]
}